scanni
iwlist wlan0 scan

tiedostoon /etc/network/interfaces

	auto lo

	iface lo inet loopback
	iface eth0 inet dhcp

	auto wlan0
	allow-hotplug wlan0
	iface wlan0 inet dhcp
		wpa-ssid "SSID"
		wpa-psk "SALASANA"



netti päälle
	sudo iwup wlan0

netti pois
	sudo iwdown wlan0